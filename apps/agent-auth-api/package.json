{
  "name": "agent-auth-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ../../packages/env/.env -- bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outfile=./dist/server.js --target=bun",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@workspace/api-utils": "workspace:*",
    "@workspace/env": "workspace:*",
    "@workspace/database": "workspace:*",
    "@workspace/logger": "workspace:*",
    "hono": "catalog:",
    "hono-pino": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "dotenv-cli": "catalog:",
    "vitest": "^4.0.18"
  }
}