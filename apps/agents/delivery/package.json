{
  "type": "module",
  "name": "delivery-agent",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ../../../packages/env/.env.agents.delivery -- bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outfile=./dist/server.js --target=bun"
  },
  "dependencies": {
    "@workspace/agent-utils": "workspace:*",
    "@workspace/env": "workspace:*",
    "@workspace/prisma": "workspace:*",
    "hono": "catalog:",
    "resend": "^6.9.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "dotenv-cli": "catalog:"
  }
}
